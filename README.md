# Camera-hacking
Access from the camera

This script is made to hack the camera through the web

👀How does it run?

first stage:
    download python

second stage:
    Install the library:
        pip install requests
        pip install pillow
        pip install base64

        
Step three:
    python app.py
